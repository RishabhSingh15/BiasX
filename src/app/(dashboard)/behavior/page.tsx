import { BehaviorView } from '@/features/behavior';

export const metadata = {
  title: 'Behavioral Analysis | BiasX',
  description: 'Track and eliminate trading habits, revenge trading, overtrading, and emotional leaks',
};

export default function BehaviorPage() {
  return <BehaviorView />;
}
